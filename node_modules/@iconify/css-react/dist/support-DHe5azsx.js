/**
* Check if browser supports `d` property in CSS
*
* On failure to check, assume that it is supported.
*/
function supportsCSS() {
	try {
		return window.CSS.supports("d: path(\"M0 0\")");
	} catch (error) {
		return true;
	}
}

export { supportsCSS as t };